// Preparation types — the page. The table and editor keep reading the older
// `items.prep.*` block; this holds what the page itself says.
export default {
    title: 'Preparation types',
    sub: '{total} managed types · shelf life, label text, waste',
};
